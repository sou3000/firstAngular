import { TriVisuelDirective } from './tri-visuel.directive';

describe('TriVisuelDirective', () => {
  it('should create an instance', () => {
    const directive = new TriVisuelDirective();
    expect(directive).toBeTruthy();
  });
});
