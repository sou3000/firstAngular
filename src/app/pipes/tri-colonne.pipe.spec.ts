import { TriColonnePipe } from './tri-colonne.pipe';

describe('TriColonnePipe', () => {
  it('create an instance', () => {
    const pipe = new TriColonnePipe();
    expect(pipe).toBeTruthy();
  });
});
